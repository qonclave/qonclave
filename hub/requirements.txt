# Qonclave hub server dependencies.
# NOTE: `geniex` is intentionally NOT listed here — it is Snapdragon-X-only and
# is imported conditionally at runtime (see hub/vlm_backend.py). Install it
# separately on the Snapdragon laptop via scripts/setup_geniex.ps1.
flask>=3.0
