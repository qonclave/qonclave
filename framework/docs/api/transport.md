# `qonclave.transport`

The transport ABCs and registry. Client libraries (paho for MQTT, etc.) stay app/hub-owned by
design — see [`CONVENTIONS.md`](../CONVENTIONS.md)'s note on why `transport.mqtt` was tried as a
real module and reverted.

::: qonclave.transport
    options:
      show_submodules: true
