# `qonclave.inference`

Model backends: the GenieX (Qualcomm Snapdragon) binding and the deterministic `MockBackend` used
for hardware-free development — see `DEVELOPER_GUIDE.md`'s "Run without hardware" section.

::: qonclave.inference
    options:
      show_submodules: true
